steps:
- name: 'gcr.io/${PROJECT_ID}/pytest_3'
  args: [ '-vv', 'tests', '--oid', '${_OID}', '--key', '${_KEY}' ]
- name: 'gcr.io/${PROJECT_ID}/pytest_2'
  args: [ '-vv', 'tests', '--oid', '${_OID}', '--key', '${_KEY}' ]