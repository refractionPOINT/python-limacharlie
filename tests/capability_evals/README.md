# Capability outcome scenarios

The 37 first-level scenarios moved with capability ownership from lc-ai. They
specify expected observable outcomes and forbidden actions across all 15
capabilities. They are a regression corpus, not a claim of 37 live executions.
The grader requires independent state/trace evidence and fails omitted runs.
Run `python tests/capability_evals/evaluate.py --help` for its input contract.
